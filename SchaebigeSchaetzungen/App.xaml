<Application x:Class="SchaebigeSchaetzungen.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SchaebigeSchaetzungen"
             xmlns:views="clr-namespace:SchaebigeSchaetzungen.View"
             xmlns:viewmodels="clr-namespace:SchaebigeSchaetzungen.ViewModel"
             StartupUri="View/MainWindow.xaml"
             >
           

    <Application.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10 5" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="2"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Helvetica"/>
            <Setter Property="FontSize" Value="18"/>
        </Style>

        <!-- <DataTemplate DataType="{x:Type viewmodels:MainViewModel}">
            <views:MainWindow/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:GameVM}">
            <views:Game/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:RegistrationVM}">
            <views:Registration/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:HighscoresVM}">
            <views:Highscores/>
        </DataTemplate>
        !-->
    </Application.Resources>
</Application>
